<ul class="right">
  <%- categories.each do |category| -%>
    <li class="has-dropdown">
      <%= link_to category.name, admin_category_path(category) %>
      <!-- begin category -->
      <ul class="dropdown">
        <li class="title back js-generated">
          <h5><a href="javascript:void(0)">Back</a></h5>
          <%= link_to category.name, admin_category_path(category) %>
        </li>
        <%- category.subcategories.each do |subcategory| -%>
          <li class="has-dropdown">
            <%= link_to subcategory.name, admin_category_path(subcategory) %>
            <ul class="dropdown">
              <li class="title back js-generated">
                <h5><a href="javascript:void(0)">Back</a></h5>
              </li>
              <%- subcategory.articles.each do |article| -%>
                <li>
                  <%= link_to article.slug, admin_category_article_path(subcategory,article) %>
                </li>
              <%- end -%>
            </ul>
          </li>
          <%- category.articles.each do |article| -%>
            <li>
              <%= link_to article.slug, admin_category_article_path(category,article) %>
            </li>
          <%- end -%>
        <%- end -%>
      </ul>
    </li>
  <%- end -%>
</ul>
